# Product

## Definition

웹 페이지 URL 하나를 깨끗한 Markdown으로 바꿔, 원하는 프롬프트와 함께 ChatGPT·Claude 대화창까지 넘겨 주는 도구.

## Users and situations

LLM으로 글을 읽고 처리하는 사람. 대부분 한국어 사용자이며, 개발자와 비개발자를 모두 포함한다.

- 긴 아티클·기술 문서·블로그 글을 발견했지만 전체를 읽을 시간이 없어 요약을 맡기려는 상황
- 외국어로 된 글을 번역해 읽으려는 상황
- 낯선 분야의 문서를 쉬운 말로 풀어 달라고 맡기려는 상황

공통점은 "읽을 것을 이미 찾았고, 읽는 일 자체를 LLM에 넘기고 싶다"는 것이다. 무엇을 읽을지 찾는 단계는 이 앱의 관심사가 아니다.

## Problem and current alternatives

LLM에 웹 문서를 넘기는 일이 매번 번거롭고 결과가 일정하지 않다. 지금은 이렇게 한다.

- **URL만 붙여넣기** — 모델이 페이지를 못 읽거나, 검색 도구로 우회해 원문과 다른 것을 읽는다. 성공 여부를 사용자가 알기 어렵다.
- **페이지 전체 선택 후 복사** — 네비게이션·광고·댓글·푸터가 함께 딸려 와 토큰을 낭비하고, 모델이 본문과 잡음을 구분하지 못한다.
- **리더 모드로 보고 복사** — 잡음은 줄지만 제목 위계, 코드블록, 링크 같은 구조가 평문으로 뭉개진다.
- **개발자용 변환 도구 사용** — 결과는 좋지만 명령어나 API를 다뤄야 해 일반 사용자에게는 선택지가 아니다.

## Promised change

문서를 LLM에 넘기기 위한 손질이 사라진다. URL을 붙여넣으면 몇 초 안에 구조가 보존된 본문만 남고, 사용자는 넘기기 전에 그것을 눈으로 확인한 뒤 원하는 프롬프트와 함께 대화창으로 보낸다. 결과가 잡음 때문에 어긋날 걱정을 하지 않아도 된다.

## Core loop

1. URL을 붙여넣는다.
2. 본문을 추출해 Markdown으로 변환한다.
3. 제목·저자 헤더와 렌더링된 Markdown으로 결과를 확인한다.
4. 내보낸다 — 복사하거나, `.md` 파일로 내려받거나, 프롬프트를 앞에 붙여 ChatGPT·Claude로 넘긴다.
5. 지우고 다음 URL로 넘어간다.

## Capabilities and boundaries

- 한 번에 URL 하나를 다룬다. 여러 URL의 일괄 변환은 하지 않는다.
- 공개적으로 접근 가능한 페이지를 대상으로 한다. 로그인·페이월·클라이언트 렌더링에 의존하는 페이지에서는 결과를 보장하지 않는다.
- 넘기기까지가 제품의 끝이다. LLM 응답을 앱 안에서 받아 보여주지 않는다.
- LLM으로 넘길 때는 문서 길이와 무관하게 항상 프롬프트와 본문 전문을 클립보드에 담고 대화창을 연다. 사용자의 붙여넣기 한 번이 흐름에 포함된다.
- 프롬프트는 소수의 고정 프리셋(요약·번역·쉽게 설명)과 1회용 직접 입력만 제공한다. 프롬프트를 저장·관리하는 기능은 없다.
- 계정과 저장소가 없다. 변환 기록, 사용자 프롬프트, 결과물 중 어떤 것도 세션을 넘겨 남지 않는다.
- 라이트·다크 모드를 모두 지원한다.

## Experience principles

- **넘기기 전에 확인할 수 있다.** 추출 결과를 사람이 읽을 수 있는 형태로 먼저 보여준다. 보이지 않는 변환은 신뢰할 수 없다.
- **한 화면, 한 흐름.** 설정, 온보딩, 계정 없이 첫 방문에서 바로 끝까지 간다.
- **같은 버튼은 항상 같게 동작한다.** 문서 길이나 출처에 따라 내보내기 동작이 달라지지 않는다.
- **실패를 숨기지 않는다.** 본문 추출이 실패하거나 빈약하면 사용자가 그 사실을 알고 다른 방법을 택할 수 있어야 한다.

## Success signals

- 변환에 성공한 사용자가 내보내기 단계까지 도달하는 비율
- 한 세션에서 두 개 이상의 URL을 처리하는 비율
- 결과물을 손보지 않고 그대로 사용하는 비율
- 본문 추출 실패·빈 본문 발생 비율이 낮게 유지되는 것

## Assumptions and unknowns

- Assumption: 사용자는 변환 결과를 눈으로 훑어 확인하고 싶어 한다. 확인 단계 없이 바로 넘기는 편을 선호할 가능성은 검증되지 않았다.
- Assumption: 붙여넣기 한 번을 감수하더라도 항상 동작하는 핸드오프가, 짧은 문서에서만 동작하는 원클릭 핸드오프보다 낫다. (사용자 확인됨)
- Assumption: 요약·번역·쉽게 설명 세 가지가 사용 의도의 대부분을 덮는다.
- Unknown: 프리셋 밖에서 반복적으로 쓰이는 의도가 있는지. 직접 입력 사용 양상이 답을 준다.
- Unknown: 로그인·페이월 페이지를 변환해 달라는 요구가 얼마나 큰지.
- Unknown: 저자·발행일 같은 메타데이터를 화면에만 보여줄지, LLM에 넘기는 본문에도 포함할지.
- Unknown: 클립보드 복사 후 붙여넣기가 실제 사용에서 마찰로 느껴지는 정도.
