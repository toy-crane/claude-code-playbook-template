# URL → Markdown 변환기

## Definition

웹 페이지 주소 하나를 LLM 대화창에 그대로 넣을 수 있는 Markdown으로 바꿔 주는, 로그인 없는 단일 페이지 도구.

## Users and situations

LLM에게 웹 문서를 근거로 묻고 싶은 사람. 개발자, 리서처, 글 쓰는 사람처럼 읽을 것이 많고 그중 상당수를 직접 읽는 대신 모델에게 넘기는 사람들이다.

주로 이런 순간에 손이 간다.

- 긴 기술 문서·블로그 글을 놓고 "요약해 줘", "우리 코드에 어떻게 적용해?"를 묻고 싶을 때.
- 링크만 붙여 넣었더니 모델이 못 읽거나, 읽은 척 엉뚱한 답을 했을 때.
- 여러 글을 한 대화의 컨텍스트로 모아 넣어야 할 때.
- 나중에 쓸 자료를 파일로 챙겨 두고 싶을 때.

## Problem and current alternatives

LLM에 URL을 그냥 주는 것은 신뢰할 수 없다. 모델이 페이지를 못 가져오기도 하고, 가져와도 네비게이션·광고·추천 목록·댓글이 본문과 섞이며, 제목·저자·발행일 같은 출처가 컨텍스트에서 사라진다. 실패가 조용해서, 사용자는 답이 틀린 뒤에야 모델이 글을 못 읽었다는 걸 안다.

그래서 지금은 이렇게들 한다.

- 브라우저에서 전체 선택 후 복사한다 — 잡음이 그대로 딸려 온다.
- 리더 모드를 켜고 복사한다 — 브라우저마다 다르고, 출처는 여전히 손으로 적어야 한다.
- 개발자용 CLI·라이브러리를 쓴다 — 설치와 실행 환경이 필요해 그때그때 쓰기 어렵다.
- 확장 프로그램을 깐다 — 브라우저와 기기에 묶인다.
- 모델의 브라우징 기능에 맡긴다 — 사이트가 막으면 실패하고, 무엇이 읽혔는지 확인할 수 없다.

## Promised change

주소 하나를 붙여 넣으면, 몇 초 안에 **붙여 넣을 준비가 된 본문과 출처**를 손에 쥔다. 무엇이 LLM에 넘어가는지 눈으로 먼저 확인할 수 있고, 넘어가지 못하는 페이지라면 그 사실을 이유와 함께 알게 된다. 설치도, 로그인도, 정리 작업도 없다.

## Core loop

URL 붙여 넣기 → 변환 → 제목·출처·본문 확인 → 복사하거나, `.md`로 내려받거나, LLM으로 넘기기 → 다음 URL.

매 회차가 독립적이다. 이전 변환이 남아 있지 않고, 사용자는 늘 빈 입력란에서 시작한다.

## Capabilities and boundaries

- 한 번에 URL 하나를 받아 결과 하나를 만든다. 일괄 변환은 하지 않는다.
- 공개 웹의 HTML 페이지 본문만 다룬다. PDF·이미지·파일, 로그인 뒤의 페이지, 자동 수집을 막는 사이트, JavaScript로만 그려지는 페이지는 변환 대상이 아니며, 이 경우 결과를 지어내지 않고 실패로 알린다.
- 페이지가 실제로 알려 주는 출처를 본문과 같은 Markdown 문서 안에 함께 실어 내보낸다. 제목과 원본 주소는 언제나 따라가고, 저자·발행일처럼 페이지에 없는 항목은 지어내지 않고 생략한다. 출처가 LLM 컨텍스트에 같이 들어가는 것이 이 도구의 존재 이유다.
- 화면에서 확인한 Markdown과 복사·다운로드·전달되는 Markdown은 언제나 동일하다.
- 계정도, 변환 이력도, 서버 저장소도 두지 않는다. 세션이 끝나면 남는 것은 사용자가 가져간 Markdown뿐이다.
- LLM으로 넘기는 일은 사용자의 붙여 넣기를 돕는 데까지다. 앱이 사용자를 대신해 모델에 요청하거나 그 응답을 다루지 않는다.
- 주소를 아는 누구나 쓸 수 있게 열려 있다. 그 대가로, 서버가 임의의 주소를 대신 가져오는 통로로 남용되지 않게 막는 일은 제품의 책임 범위 안에 있다.
- 추출한 타인의 글을 앱이 보관·재배포·공개하지 않는다. 결과물은 그 자리에서 사용자에게만 간다.
- 편집기가 아니다. 결과를 다듬는 일은 붙여 넣은 곳에서 한다.

## Experience principles

- 빈 화면이 스스로 무엇을 넣는 곳인지 설명한다.
- 실패를 정직하게 구분해 말한다. 특히 깨진 글자나 빈 껍데기를 성공으로 내보내지 않는다 — 잘못된 성공이 실패보다 나쁘다.
- 넘어갈 것을 미리 볼 수 있다. 사용자가 확인하지 않은 내용이 클립보드로 나가지 않는다.
- 한 화면에서 끝난다. 설정·단계·모드를 만들지 않는다.
- 기다림은 짧고, 기다리는 동안 무슨 일이 일어나는지 보인다.

## Success signals

- 변환에 성공한 세션이 복사·다운로드·전달 중 하나까지 도달한다. 결과를 보기만 하고 떠나는 것은 목적 달성이 아니다.
- 같은 방문에서 두 번째, 세 번째 URL을 변환한다. 도구가 작업 흐름 안에 들어갔다는 신호다.
- 변환 실패 중 원인을 특정할 수 있는 실패의 비중이 높다. 정체불명의 실패가 늘어나면 추출 범위나 오류 구분이 현실과 어긋난 것이다.
- 결과를 손대지 않고 붙여 넣어도 LLM이 제대로 답한다. 사용자가 앞뒤를 잘라내야 한다면 추출이 덜 된 것이다.

## Assumptions and unknowns

- Assumption: 사용자는 URL을 그대로 주는 것보다 정제된 Markdown을 주는 편이 낫다고 체감한다.
- Assumption: 출처 메타데이터를 항상 함께 내보내는 편이 대부분의 상황에서 유용하다. 빼고 싶다는 요구는 아직 확인되지 않았다.
- Assumption: 전달은 클립보드에 담아 대화창을 열어 주는 정도로 충분하다. 본문 길이 때문에 주소로 프롬프트를 미리 채우는 방식은 성립하지 않는다.
- Assumption: 여기서 말하는 LLM의 기본 대상은 ChatGPT와 Claude다.
- Assumption: UI 언어는 한국어다.
- Unknown: 자동 수집을 막는 사이트와 JavaScript로만 그려지는 페이지가 실사용에서 어느 정도 비중인지. 이 비중이 크면 "공개 웹 HTML만" 이라는 경계 자체를 다시 봐야 한다.
- Unknown: 계정도 이력도 없는 앱에서 위 성공 신호를 어떤 방식으로 관측할지.
- Unknown: 공개된 상태로 타인의 글 본문을 추출하는 데 대해 어디까지 정책 검토가 필요한지.
- Unknown: 여러 URL을 한 번에 넘기려는 요구가 실제로 존재하는지. 현재는 없다고 보고 경계를 그었다.
